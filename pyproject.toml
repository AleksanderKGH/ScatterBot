[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    "__pycache__",
    ".git",
]

[tool.ruff.lint]
# Keep this starter profile focused on high-signal issues.
# It catches import errors and likely runtime/syntax bugs while staying tolerant.
select = ["E9", "F63", "F7", "F82"]
